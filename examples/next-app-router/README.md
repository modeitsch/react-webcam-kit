# Next.js App Router Starter

A minimal Next.js App Router starter that keeps browser camera APIs inside a client component.

## Run

```bash
npm install
npm run dev
```

Next.js serves the app at `http://localhost:3000` by default.
