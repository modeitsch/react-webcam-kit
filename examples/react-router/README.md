# React Router Starter

A small React Router starter with a camera route and a recorder route using `react-webcam-kit`.

## Run

```bash
npm install
npm run dev
```

Vite serves the app at `http://localhost:5173` by default.
