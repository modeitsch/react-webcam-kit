# Camera Recorder Starter

A copy-ready Vite example for camera preview, screenshot capture, video recording, playback, and
download.

```bash
cd examples/camera-recorder
npm install
npm run dev
```

The example uses the local package through `file:../..`, so it is useful for both package users and
maintainers testing changes before release.
