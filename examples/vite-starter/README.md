# Vite Starter

A small Vite + React + TypeScript starter for camera preview, screenshot capture, video recording,
and safe Blob URL cleanup.

## Run

```bash
npm install
npm run dev
```

Vite serves the app at `http://localhost:5173` by default.
